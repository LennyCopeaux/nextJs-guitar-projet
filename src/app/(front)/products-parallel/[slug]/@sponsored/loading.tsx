import { SectionSkeleton } from "@/components/ui/section-skeleton";

export default function SponsoredParallelLoading() {
  return <SectionSkeleton title="Slot @sponsored en chargement..." lines={4} />;
}
