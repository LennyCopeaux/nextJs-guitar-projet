import { SectionSkeleton } from "@/components/ui/section-skeleton";

export default function SimilarParallelLoading() {
  return <SectionSkeleton title="Slot @similar en chargement..." lines={4} />;
}
